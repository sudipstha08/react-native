import React from 'react'
import { View } from 'react-native'

const Divider = ({width, height,backgroundColor}) => {
  return (
    <View style={{ width, height, backgroundColor}}/>
  )
}

export  {Divider}